# Generate with: openssl rand -base64 32
AUTH_SECRET=replace-with-a-long-random-secret

# Set only while creating the first administrator, then remove it.
ADMIN_BOOTSTRAP_TOKEN=replace-with-a-different-long-random-secret

# Public canonical origin used by Better Auth.
BETTER_AUTH_URL=https://www.zaviarcade.com
